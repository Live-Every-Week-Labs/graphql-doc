---
id: notification-received
title: notificationReceived
sidebar_label: notificationReceived
tags: ["notifications", "realtime", "auth-required"]
hide_title: true
api: true
---

import operationsByType from '../_data/operations.json';
import typesByName from '../_data/types.json';

export const examplesByOperation = {
  "notificationReceived": (operationsByType["subscription"]["notificationReceived"]?.examples ?? []),
};

export const operation = operationsByType["subscription"]["notificationReceived"];

<OperationView operation={ operation } typesByName={typesByName} headingLevel={ 1 } typeLinkBase="../types">
Subscribe to notifications for the current user

Receives real-time notifications including order updates,
price alerts, stock alerts, and system messages.
</OperationView>
