---
id: users
title: users
sidebar_label: users
tags: ["users", "read", "admin-only", "deprecated"]
hide_title: true
api: true
---

import operationsByType from '../../_data/operations.json';
import typesByName from '../../_data/types.json';

export const examplesByOperation = {
  "users": (operationsByType["query"]["users"]?.examples ?? []),
};

export const operation = operationsByType["query"]["users"];

<OperationView operation={ operation } typesByName={typesByName} headingLevel={ 1 } typeLinkBase="../../types">
Search for users (admin only)
</OperationView>
