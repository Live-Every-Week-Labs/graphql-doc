---
id: place-order
title: placeOrder
sidebar_label: placeOrder
tags: ["orders", "write", "auth-required", "payment"]
hide_title: true
api: true
---

import operationsByType from '../../_data/operations.json';
import typesByName from '../../_data/types.json';

export const examplesByOperation = {
  "placeOrder": (operationsByType["mutation"]["placeOrder"]?.examples ?? []),
};

export const operation = operationsByType["mutation"]["placeOrder"];

<OperationView operation={ operation } typesByName={typesByName} headingLevel={ 1 } typeLinkBase="../../types">
Place an order from the current cart

Returns either an Order on success, CheckoutError on failure,
or PaymentPending if payment is still processing.
</OperationView>
